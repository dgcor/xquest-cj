# stdlog

stdlog is a Cangjie package that implements logging using `stdx.log`.  

This is a copy of `stdx.log` separate from `stdx`. Changes from the original:

* namespace change: `stdx.log.*` -> `stdlog.*`
* namespace change: `stdx.logger.*` -> `stdlog.logger.*`
* removed: JsonLogger

## License

Apache License
